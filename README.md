- Requires gfortran, gcc and gsl

- Set gsl_path to the path where gsl is installed (typically either /usr or /usr/local)

- Requires Python 3.6+

- Requires patched SymPy https://github.com/ChrisPattison/sympy